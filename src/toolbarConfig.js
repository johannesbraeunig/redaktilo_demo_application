import { buttonConfig as boldButton } from "redaktilo-plugin-bold";
import { buttonConfig as italicButton } from "redaktilo-plugin-italic";

const buttons = [[boldButton(), italicButton()]];

export { buttons };
